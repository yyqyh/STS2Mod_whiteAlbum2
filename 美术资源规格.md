# STS2_WhiteAlbum2 静态 PNG 规格表（美术出图用）

> 用途：把「要画哪些图、每张多大」一次性给全，照着出图后**同名覆盖**即可生效。
>
> 尺寸依据三份交叉验证，全部是实测值，不是估计：
>
> | 依据 | 路径 |
> |---|---|
> | 参考 mod 酒狐 | `E:\game_part2\mod_c\sts2\base\STS2_WineFox-1.2.18\STS2_WineFox-1.2.18\` |
> | 参考 mod 堕化拉撒路（同为 RitsuLib + 静态图） | `E:\game_part2\mod_c\sts2\mod\remake\tainted-lazarus\Tainted Lazarus\` |
> | 本体反编译工程（原版权威尺寸） | `E:\game_part2\mod_c\sts2\mod\orign\` |
>
> 本 mod 代码里实际读取的路径常量在 `Core/Character/AlbumCharacterArt.cs`、
> `Core/Settings/...`、`Core/Cards/AlbumCardArt.cs`；资源根为 `res://STS2_WhiteAlbum2/`。

---

## 0. 尺寸速查（最常用）

| 尺寸 | 用在哪 |
|---:|---|
| 264 × 390 | 选人立绘（本体 132×195 的 2×，高清） |
| 132 × 195 | 选人立绘（本体原始尺寸，1×） |
| 88 × 88 | 顶栏头像 / 头像描边 / 先古简史图标 |
| 49 × 64 | 地图角色标记 |
| 2564 × 1204 | 选人背景（本体实测；出 2560×1200 也够） |
| 726 × 1050 | 战斗立绘（待机/攻击/受击/死亡） |
| 450 × 642 | 死亡结算立绘 |
| 24 × 24 | 能量小图标（文字行内） |
| 64 × 64 | 能量大图标 / 能力图标 / 附魔图标 |
| 256 × 256 | 能量计数器 5 层球体 / 遗物 / 药水 / 能力（master） |
| 512 × 512 | 能量爆发、闪光粒子 |
| 1000 × 760 | 卡面（master） |
| 500 × 380 | 卡面（小档） |
| 3440 × 1616 | 事件立绘 |
| 2560 × 1200 | 转场遮罩、事件前景 |
| 278 × 278 | 先古地图节点（含 `_outline`） |
| 422 × 1200 | 联机手势（指/石头/布/剪刀） |
| 500 × 500 | Mod 封面 |

---

## 1. 角色静态资源（Setsuna / Touma 各一套，共 2 套）

路径模板：`res://STS2_WhiteAlbum2/images/character/<角色>/…`（`<角色>` = `setsuna` / `touma`）

| # | 用途 | 文件名 | 推荐尺寸 (px) | 本体/参考实测 | 备注 |
|---:|---|---|---:|---|---|
| 1 | 选人立绘（已解锁） | `character_select_<角色>.png` | **264 × 390** | 本体 `char_select_silent.png` 132×195；WineFox / Lazarus 均出 264×390 | 2× 高清，游戏内显示区约 88×133 |
| 2 | 选人立绘（锁定） | `character_select_<角色>_locked.png` | **264 × 390** | 同上 | 灰度+锁标可选，锁标本体自带 |
| 3 | 顶栏角色头像 | `character_icon_<角色>.png` | **88 × 88** | 本体 `images/ui/top_panel/character_icon_silent.png` 88×88 | 透明底 |
| 4 | 顶栏头像描边 | `character_icon_<角色>_outline.png` | **88 × 88** | 本体 `..._outline.png` 88×88 | 纯白描边，叠色用 |
| 5 | 地图标记 | `map_marker_<角色>.png` | **49 × 64** | 本体 `images/packed/map/icons/map_marker_silent.png` | 竖版小人头 |
| 6 | 选人背景 | `select_bg.png` | **2564 × 1204**（出 2560×1200 亦可） | 本体 `animations/character_select/silent/character_select_silent_bg.png` 2564×1204 | 被 `select_bg_<角色>.tscn` 引用，满屏拉伸 |
| 7 | 战斗立绘·待机 | `combat_idle.png` | **726 × 1050** | Lazarus `combat_idle.png` 726×1050；WineFox `placeholder.png` 也是 726×1050 | 透明底，脚底对齐画布底 |
| 8 | 战斗立绘·攻击 | `combat_attack.png` | **726 × 1050** | Lazarus | 同上，构图建议与待机同位 |
| 9 | 战斗立绘·受击 | `combat_hit.png` | **726 × 1050** | Lazarus | 同上 |
| 10 | 战斗立绘·死亡 | `combat_dead.png` | **726 × 1050** | Lazarus | 同上 |
| 11 | 死亡结算立绘 | `character_die.png` | **450 × 642** | WineFox `winefox_die.png` 450×642 | 结算画面用 |
| 12 | 能量小图标 | `energy_icon.png` | **24 × 24** | 本体 `images/packed/sprite_fonts/silent_energy_icon.png` 24×24 | 文案行内图标 |
| 13 | 能量大图标 / 卡池图标 | `energy_icon_big.png` | **64 × 64** | Lazarus `images/ui/energy_tainted_lazarus.png` 64×64 | 卡池/遗物池 `BigEnergyIconPath` 用 |
| 14 | 商店立绘 | `shop.png` | **1792 × 2400** | WineFox `winefox_shop.png` 1792×2400 | 静态替代时建议改 726×1050 与战斗同构图 |
| 15 | 休息处立绘 | `rest.png` | **967 × 1792** | WineFox `winefox_rest.png` 967×1792 | 被 `<角色>_rest_site.tscn` 引用 |
| 16 | 休息处篝火 | `restsite/campfire.png` | **1000 × 1000** | WineFox `winefox/restsite/酒狐篝火.png` 1000×1000 | 被 rest site 场景引用（若走篝火式表现） |
| 17 | 卡牌轨迹·主带 | `vfx/trail.png` | **32 × 32** | 本体 `images/packed/vfx/trail.png` 32×32 | 横向可平铺、灰度 |
| 18 | 卡牌轨迹·副带 | `vfx/trail2.png` | **64 × 64** | 本体 `images/packed/vfx/trail2.png` 64×64 | 同上 |
| 19 | 卡牌轨迹·残影 | `vfx/small_card_silhouette.png` | **64 × 64** | 本体 `images/packed/vfx/small_card_silhouette.png` | 卡牌残影剪影 |
| 20 | 转场遮罩 | `images/ui/transitions/<角色>_transition.png` | **2560 × 1200** | 本体 `silent_transition.png` 2560×1200 | **灰度图，只用 R 通道**；黑=先擦除 |
| 21 | 联机手势·指 | `arm/point.png` | **422 × 1200** | 本体 `images/ui/hands/multiplayer_hand_silent_point.png` 422×1200；WineFox 出 308×864 | 竖长条，手在画布内 |
| 22 | 联机手势·石头 | `arm/rock.png` | **422 × 1200** | 同上 | |
| 23 | 联机手势·布 | `arm/paper.png` | **422 × 1200** | 同上 | |
| 24 | 联机手势·剪刀 | `arm/scissors.png` | **422 × 1200** | 同上 | |
| 25 | 兜底占位立绘 | `placeholder.png` | **726 × 1050** | WineFox `winefox/placeholder.png` 726×1050 | 资源缺失时顶上，可不做 |

**每角色 25 张 → 两角色 50 张。**

---

## 2. tscn 中所需的 PNG（场景 ↔ 贴图对照）

### 2.1 本项目**已有**的场景

| tscn 文件 | 节点与尺寸 | 引用的 PNG | PNG 尺寸 |
|---|---|---|---|
| `scenes/char_select/select_bg_setsuna.tscn` | `Control` 满屏 → `TextureRect` 满屏，`expand_mode=1` `stretch_mode=6` | `images/character/setsuna/select_bg.png` | 2564 × 1204 |
| `scenes/char_select/select_bg_touma.tscn` | 同上 | `images/character/touma/select_bg.png` | 2564 × 1204 |

### 2.2 本项目**待新建**的场景（做静态替换时要建，路径由 `AssetProfile` 指向）

| tscn 文件 | 结构（照 Lazarus / 本体同款） | 引用的 PNG | PNG 尺寸 |
|---|---|---|---|
| `scenes/combat/<角色>_combat.tscn` | `Node2D` → `Sprite2D`（`position=(0,-140)`、`scale=0.6`）+ `Control Bounds`（250 × 280）+ `Marker2D CenterPos (0,-140)` + `Marker2D IntentPos (0,-350)` | `combat_idle.png`（可扩展 attack/hit/dead） | 726 × 1050 |
| `scenes/shop/<角色>_shop.tscn` | `Node2D` → `Sprite2D`（本体商人 scale≈0.47） | `shop.png` | 1792 × 2400（或统一 726×1050） |
| `scenes/rest_site/<角色>_rest_site.tscn` | `Node2D` → `Sprite2D`；再实例化 `res://scenes/ui/selection_reticle.tscn` | `rest.png` / `restsite/campfire.png` | 967 × 1792 / 1000 × 1000 |
| `scenes/ui/character_icons/<角色>_icon.tscn` | `TextureRect` `anchors_preset=15`，`expand_mode=1` `stretch_mode=5` | `character_icon_<角色>.png` | 88 × 88 |
| `ui/energy_counters/<角色>_energy_counter.tscn` | `Control` 128×128，`pivot_offset=(64,64)`；`Layers` 内 5 张 `TextureRect`；`BurstBack/Front` 为 `CPUParticles2D`；`Label` `offset_left=16, offset_top=-29` | 5 层球体 + 爆发 + 闪光 | 256×256 ×5、512×512 ×2 |
| `scenes/vfx/card_trail_<角色>.tscn` | `Node2D` → `Line2D` ×2（`width` 96 / 64，`texture_mode=2`）+ 两组 `CPUParticles2D` + 2 个 `Sprite2D` | `vfx/trail.png`、`vfx/trail2.png`、`vfx/small_card_silhouette.png` | 32×32、64×64、64×64 |
| `materials/transitions/<角色>_transition_mat.tres` | `ShaderMaterial`（内联 4 行 shader，只采样 `.r`） | `images/ui/transitions/<角色>_transition.png` | 2560 × 1200 |
| `scenes/ui/selection_reticle.tscn`（可选，复用本体） | `Control` + 4 个 `TextureRect`（三角标记） | `images/ui/combat/combat_reticle.png` | 48 × 48 |

> `scenes/ui/selection_reticle.tscn` 与 `images/ui/combat/combat_reticle.png` 本体已存在，
> 不自己出图就直接引用本体路径即可。

---

## 3. 能量计数器（战斗右下角球体）

两套风格二选一，尺寸都列出来：

### 3.1 层叠球体式（本体 / Lazarus 风格，推荐）

| 用途 | 文件名 | 推荐尺寸 | 实测依据 |
|---|---|---:|---|
| 球体第 1 层（底） | `<角色>_orb_layer_1.png` | 256 × 256 | 本体 `images/ui/combat/energy_counters/silent/silent_orb_layer_1.png` 256×256 |
| 球体第 2 层（旋转） | `<角色>_orb_layer_2.png` | 256 × 256 | 同上 |
| 球体第 3 层（旋转） | `<角色>_orb_layer_3.png` | 256 × 256 | 同上 |
| 球体第 4 层 | `<角色>_orb_layer_4.png` | 256 × 256 | 同上 |
| 球体第 5 层（高光） | `<角色>_orb_layer_5.png` | 256 × 256 | 同上 |
| 能量爆发粒子 | `energy_orb_burst.png` | 512 × 512 | 本体 `energy_burst/energy_orb_burst.png` |
| 能量闪光粒子 | `energy_orb_shine.png` | 512 × 512 | 本体 `energy_burst/energy_orb_shine.png` |

### 3.2 齿轮式（WineFox 风格，可选）

| 用途 | 文件名 | 推荐尺寸 |
|---|---|---:|
| 齿轮顶部 | `gear_top.png` | 600 × 600 |
| 齿轮底部 | `gear_bottom.png` | 434 × 434 |
| 齿轮 2 | `gear_2.png` | 328 × 344 |
| 齿轮 3 | `gear_3.png` | 237 × 246 |
| 计数器小图标 | `energy_counter_<角色>.png` | 128 × 128 |

---

## 4. 卡牌

| 用途 | 推荐尺寸 | 实测依据 |
|---|---:|---|
| 卡面 master | **1000 × 760** | 本体 `images/packed/card_portraits/<角色>/<卡>.png` |
| 卡面小档 | **500 × 380** | 本体同类（部分旧卡） |
| 全画幅特殊卡 | **606 × 852** | 本体 `images/packed/card_portraits/ancient_beta.png` 606×852 |
| 卡牌库小图（可选） | **244 × 184** | 本体 `images/packed/card_portraits/beta.png` |
| RitsuLib 通用占位卡面 | 1000 × 760 | `STS2RitsuLib/card_art_placeholder.png` |
| 卡池/能量徽记（帧材质） | 512 × 512 | 复用能量小球即可 |

### 需要出的数量

| 分组 | 张数 | 是否必出 |
|---|---:|---|
| Setsuna 正式牌 `SetsunaCardOne` ~ `Five` | 5 | 必出 |
| Touma 正式牌 `ToumaCardOne` ~ `Five` | 5 | 必出 |
| Setsuna 保护性占位牌 `SetsunaTemp*` | 33 | 可复用（同稀有度×类型共用一张，最少 11 张） |
| Touma 保护性占位牌 `ToumaTemp*` | 33 | 同上 |
| **合计** | **76** | 最少可压到 **10 + 22 = 32 张** |

建议路径：`res://STS2_WhiteAlbum2/images/cards/<角色>/<卡名小写蛇形>.png`
占位牌建议路径：`res://STS2_WhiteAlbum2/images/cards/<角色>/temp/<稀有度>_<类型>.png`

> 占位牌当前**不写 `cards.json`**，只是占住卡池位置，可以一直用同一张图。

---

## 5. 其他内容静态 PNG

| 类型 | 用途 | 推荐尺寸 | 实测依据 | 本 mod 需要几个 |
|---|---|---:|---|---:|
| 能力图标（master） | `PowerAssetProfile` | 256 × 256 | 本体 `images/powers/<名>_power.png` 256×256 | 目前 0（预留） |
| 能力图标（游戏内档） | 同上 | 64 × 64 | WineFox `powers/*.png` 64×64；本体图集区域约 56×64 | 同上 |
| 遗物图标 | `IconPath` | 256 × 256 | 本体 `images/relics/<名>.png` 256×256 | 12 个注册，现复用 3 张 → 最少 3 |
| 遗物描边 | `IconOutlinePath` | 256 × 256 | 本体 `relic_outline_atlas` | 同上 |
| 遗物检视大图 | 检视界面 | 408 × 408（框） | 本体 `packed/inspect_relic_screen/relic_inspect_frame.png` 408×408 | 0（用本体框） |
| 药水图标 | `PotionAssetProfile` | 256 × 256 | 本体 `images/potions/*.png` 256×256 | 1 |
| 药水描边 | `OutlinePath` | 256 × 256 | 本体 `potion_outline_atlas` | 1 |
| 事件立绘 | `EventAssetProfile.InitialPortraitPath` | **3440 × 1616** | 本体 `images/events/*.png` 全为 3440×1616 | 1（决斗邀请） |
| 事件背景/前景 | 事件场景 | 2560 × 1200 | 本体 `images/events/dense_vegetation_foreground.png` | 0（用本体） |
| 先古地图节点 | `AncientEventPresentationAssetProfile.MapIconPath` | **278 × 278** | 本体 `images/packed/map/ancients/ancient_node_neow.png` 278×278 | 2（本体+描边） |
| 先古简史图标 | `RunHistoryIconPath` | **88 × 88** | 本体 `images/ui/run_history/neow.png` 88×88 | 2 |
| 房间/分类简史图标 | run history | 128 × 128 | 本体 `run_history/{monster,elite,event,shop,rest_site,treasure}.png` 128×128 | 0（用本体） |
| 附魔图标 | `EnchantmentAssetProfile` | 64 × 64 | WineFox `enchantments/*.png` 64×64 | 0（预留） |
| Mod 封面 | 清单/创意工坊 | 500 × 500 | RitsuLib `mod_image.png` 500×500 | 1 |
| 地图 ping 标记 | 联机协作 | 128 × 128 | 本体 `images/packed/map/icons/map_ping.png` | 0（用本体） |

---

## 6. 目录树（照着建文件夹即可）

```text
STS2_WhiteAlbum2/
├─ images/
│  ├─ character/
│  │  ├─ setsuna/
│  │  │  ├─ character_select_setsuna.png          264 × 390
│  │  │  ├─ character_select_setsuna_locked.png   264 × 390
│  │  │  ├─ character_icon_setsuna.png            88 × 88
│  │  │  ├─ character_icon_setsuna_outline.png    88 × 88
│  │  │  ├─ map_marker_setsuna.png                49 × 64
│  │  │  ├─ select_bg.png                         2564 × 1204
│  │  │  ├─ combat_idle.png                       726 × 1050
│  │  │  ├─ combat_attack.png                     726 × 1050
│  │  │  ├─ combat_hit.png                        726 × 1050
│  │  │  ├─ combat_dead.png                       726 × 1050
│  │  │  ├─ character_die.png                     450 × 642
│  │  │  ├─ energy_icon.png                       24 × 24
│  │  │  ├─ energy_icon_big.png                   64 × 64
│  │  │  ├─ shop.png                              1792 × 2400
│  │  │  ├─ rest.png                              967 × 1792
│  │  │  ├─ placeholder.png                       726 × 1050
│  │  │  ├─ restsite/campfire.png                 1000 × 1000
│  │  │  ├─ vfx/trail.png                         32 × 32
│  │  │  ├─ vfx/trail2.png                        64 × 64
│  │  │  ├─ vfx/small_card_silhouette.png         64 × 64
│  │  │  └─ arm/{point,rock,paper,scissors}.png   422 × 1200
│  │  └─ touma/  （同上，文件名换 touma）
│  ├─ cards/
│  │  ├─ setsuna/**.png                           1000 × 760
│  │  ├─ setsuna/temp/<稀有度>_<类型>.png          1000 × 760
│  │  ├─ touma/**.png                             1000 × 760
│  │  └─ touma/temp/<稀有度>_<类型>.png            1000 × 760
│  ├─ powers/**.png                               256 × 256
│  ├─ relics/**.png                               256 × 256
│  ├─ potions/**.png                              256 × 256
│  ├─ events/duel_invitation.png                  3440 × 1616
│  ├─ ui/
│  │  ├─ transitions/{setsuna,touma}_transition.png   2560 × 1200
│  │  ├─ energy_counters/<角色>_orb_layer_1..5.png    256 × 256
│  │  ├─ energy_counters/energy_orb_burst.png         512 × 512
│  │  └─ energy_counters/energy_orb_shine.png         512 × 512
│  └─ mod_image.png                               500 × 500
├─ scenes/
│  ├─ char_select/select_bg_{setsuna,touma}.tscn      （已存在）
│  ├─ combat/{setsuna,touma}_combat.tscn              （待建）
│  ├─ shop/{setsuna,touma}_shop.tscn                  （待建）
│  ├─ rest_site/{setsuna,touma}_rest_site.tscn        （待建）
│  └─ vfx/card_trail_{setsuna,touma}.tscn             （待建）
├─ ui/
│  ├─ character_icons/{setsuna,touma}_icon.tscn       （待建）
│  └─ energy_counters/{setsuna,touma}_energy_counter.tscn（待建）
└─ materials/transitions/{setsuna,touma}_transition_mat.tres（待建）
```

---

## 7. 出图备注

1. **一律 PNG-32（带 alpha）**，暗背景上叠色，不要烘焙背景色。
2. 战斗立绘、手势、轨迹、转场遮罩这几类**必须透明底**；转场遮罩是**灰度**，程序只取 `R` 通道。
3. 战斗立绘的**脚底对齐画布底边**，左右居中；这样 `position=(0,-140)` + `scale=0.6` 直接可用，不用逐张调锚点。
4. 两张角色图**构图要一致**（同画布、同视线高度），否则 together 双人站一起会一高一矮。
5. 选人立绘实际显示只有 88×133，出 264×390 是为了高分屏清晰；出 132×195 也不会错位。
6. 卡面统一 1000×760；占位牌可以只做「稀有度 × 类型」11 张再复用。
7. 名字**严格同名覆盖**：现在的占位图路径就是这个表里的路径，文件名不改就直接生效。
8. 图片改完要重新导出 PCK（`dotnet build .\STS2_WhiteAlbum2.sln -c Debug` 会自动导）。

---

## 8. 占位图（已生成）

生成脚本（工作区）：`tools/gen_placeholder_art.ps1`

```powershell
pwsh -File .\tools\gen_placeholder_art.ps1
```

**结果：123 张，470,962 字节（≈ 460 KB），平均 3.8 KB/张，全部为指定尺寸的纯色 RGBA PNG。**

| 目录 | 张数 | 说明 |
|---|---:|---|
| `images/character/setsuna` | 18 | 角色主目录 |
| `images/character/setsuna/arm` | 4 | 联机手势 |
| `images/character/setsuna/restsite` | 1 | 篝火 |
| `images/character/setsuna/vfx` | 3 | 卡牌轨迹 |
| `images/character/touma/**` | 26 | 同上（Touma 一套） |
| `images/ui/energy_counters` | 18 | 2×5 层球体 + 2 计数器 + 6 粒子/齿轮 |
| `images/ui/transitions` | 2 | 转场遮罩（纯白灰度） |
| `images/cards/setsuna` + `temp` | 16 | 5 正式 + 11 占位 |
| `images/cards/touma` + `temp` | 16 | 同上 |
| `images/relics` | 6 | 3 图标 + 3 描边 |
| `images/potions` | 2 | 1 图标 + 1 描边 |
| `images/events` | 1 | 决斗邀请立绘 |
| `images/packed/map/ancients` | 6 | 3 先古节点 + 3 描边 |
| `images/ui/run_history` | 3 | 3 先古简史图标 |
| `images` | 1 | Mod 封面 |

### 实际落地的文件名

| 分类 | 文件名规则 |
|---|---|
| 卡牌 | `images/cards/<角色>/card_{one..five}.png` |
| 占位牌 | `images/cards/<角色>/temp/{basic,common,uncommon,rare}_{attack,skill,power}.png`（Basic 无 Power，11 张/角色） |
| 遗物 | `images/relics/relic_album_{one,two,three}.png` + `_outline.png` |
| 药水 | `images/potions/potion_album_one.png` + `_outline.png` |
| 事件 | `images/events/duel_invitation.png` |
| 先古节点 | `images/packed/map/ancients/ancient_node_{act_one,act_two,act_three}_ancient.png` + `_outline.png` |
| 先古简史 | `images/ui/run_history/{act_one,act_two,act_three}_ancient.png` |
| 封面 | `images/mod_image.png` |

### 占位配色（进游戏一眼分辨是哪类资源没画）

| 颜色 | 分类 |
|---|---|
| 粉紫 | 角色本体图 |
| 蓝 | 卡面 |
| 金 | 遗物 |
| 紫红 | 药水 |
| 紫 / 亮紫 | 事件 / 先古 |
| 青 | 能量计数器与粒子 |
| 纯白不透明 | 所有 `_outline` 与转场遮罩 |
| 深蓝不透明 | Mod 封面 |

> 占位图是**实心色块**，只为了消掉 `Missing resource path` 并在游戏里看得见占位区域；
> 同名覆盖成正式图后重新导 PCK 即可，不需要改任何代码。
